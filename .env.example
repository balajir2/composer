# Composer — environment variable template.
# Copy this file to .env and fill in the values.

# ─── App ──────────────────────────────────────
APP_NAME=composer
ENVIRONMENT=development
LOG_LEVEL=INFO

# ─── Database ─────────────────────────────────
# Primary path: Neon (serverless Postgres)
#   postgresql://<user>:<password>@<host>.neon.tech/<dbname>?sslmode=require
# Fallback path: local Docker Postgres (run `docker compose up -d`)
#   postgresql://composer:composer@localhost:5432/composer
DATABASE_URL=

# ─── Auth ─────────────────────────────────────
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_ACCESS_TTL_SECONDS=3600
JWT_REFRESH_TTL_SECONDS=604800

# ─── Encryption (AES-256-GCM) ─────────────────
# 32-byte base64 key for stored secrets. Generate with:
# python -c "import base64, os; print(base64.b64encode(os.urandom(32)).decode())"
ENCRYPTION_KEY=

# ─── LangSmith tracing (optional) ─────────────
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=composer
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
